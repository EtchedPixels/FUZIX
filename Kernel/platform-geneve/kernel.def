; FUZIX mnemonics for memory addresses etc

; (this is struct u_data from kernel.h)
; 256+256 bytes.
U_DATA__TOTALSIZE       .equ 0x200

PROGLOAD		.equ 0x0100
DPBASE			.equ 0x20


IRQ_ON			.equ 5
IRQ_OFF			.equ 0
